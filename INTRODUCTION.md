# 无法收敛 (Unable to Converge)

## 项目简介

《无法收敛》是一款基于原生 Web 技术构建的 **2D 叙事解谜网页游戏**。作品将工程领域中"输入 → 模型 → 输出"的系统概念转译为亲密关系中的误解装置——白天，玩家面对的是可以被拆解、验证并收敛的具体问题；夜晚，语言进入他人的经验、偏见与情绪，被不可见的函数重新映射，越解释越偏离预期，越努力越像一场无法收敛的训练。

游戏以 4 天为周期，在白昼与夜晚之间交替推进，记录一场关于表达、误读与停止解释的沟通实验。

## 核心体验

### 白天：可解决的问题

白天场景里，玩家需要处理具体的工作与生活事务：

| 天数 | 任务 | 步骤 |
|------|------|------|
| 第 1 天 | 修电脑网络问题 | 检查网络 → 重启路由器 → 重新连接 |
| 第 2 天 | 修改工作文档 | 拆分段落 → 调整顺序 → 突出结论 |
| 第 3 天 | 整理房间 | 分类物品 → 丢弃垃圾 → 归位收纳 |
| 第 4 天 | 准备汇报 | 整理提纲 → 制作幻灯片 → 练习表达 |

操作方式：**拖拽场景中的高亮物件到虚线目标区域**，按合理顺序推进。每完成一步，问题向答案靠近一步——原因明确，反馈清晰，步骤可验证。

### 夜晚：不断偏移的误解

夜晚场景里，玩家面对家人、朋友、同事或亲密关系中的人。你说出一句话，对方听到的却是另一句——语言被对方的情绪函数重新映射，真实意图开始偏离预期输出。

玩家需要在四种回应策略中做出选择：

| 策略 | 快捷键 | 效果 |
|------|--------|------|
| **更详细地解释** | `1` | 清晰度 ↑ / 对方理解 ↓ / 解释成本 ↑ / 自我保留度 ↓ |
| **更委婉地表达** | `2` | 清晰度 ↑ / 对方理解 ↓ / 解释成本 ↑ / 自我保留度 ↓ |
| **暂停解释，保护自己** | `3` | 解释成本 ↓ / 自我保留度 ↑ |
| **换个角度重新表达** | `4` | 清晰度 ↑ / 对方理解 ↑ / 解释成本 ↑ / 自我保留度 ↓ |

每一次选择都会改变四项数值。而误解未必消失——对方可能把更详细的解释听成责备，把更委婉的表达听成试探，把暂停听成逃避。

### 四个数值系统

- **表达清晰度**：你说得是否足够清楚。
- **对方理解率**：对方是否接住了你的意思。
- **解释成本**：你已经为说明动机和善意付出了多少。
- **自我保留度**：你是否仍然保留着自己的边界。

四项数值在第 4 天结束时决定结局。

### 三种结局

| 结局 | 触发条件 | 描述 |
|------|----------|------|
| 结局一：无限解释 | 解释成本 ≥ 80 或 自我保留度 ≤ 25 | 你终于学会了如何不引发争执。代价是，你越来越少说出真实的自己。 |
| 结局二：沉默系统 | 清晰度 ≤ 40 且 理解率 ≤ 40 | 系统稳定了，因为它不再接收输入。没有争执，也没有理解。 |
| 结局三：边界重构 | 其他情况 | 你仍然无法让所有人理解你。但你终于明白，无法收敛的关系，不一定需要继续训练。 |

## 交互方式

| 操作 | 说明 |
|------|------|
| 🖱️ 鼠标拖拽 | 拖拽高亮物件到虚线目标区域 |
| 🖱️ 鼠标点击 | 点击按钮触发事件 |
| ⌨️ `1` `2` `3` `4` | 夜晚快速选择回应策略 |
| ⌨️ `Tab` | 打开/关闭决策日志 |
| ⌨️ `S` | 开关音效 |
| ⌨️ `Enter` / `Space` | 推进结果页面 |
| 📱 触屏 | 轻触按钮 / 长按拖动物件 |

## 技术实现

### 技术栈

- **HTML5 Canvas 2D API**：所有场景、角色、按钮和 UI 均在 Canvas 上绘制，不依赖 DOM UI 组件
- **原生 JavaScript (ES5+)**：包含 Safari 兼容性 polyfill（`Object.fromEntries`、`aspect-ratio` 降级）
- **Web Audio API**：使用振荡器合成音效（点击、成功、失败、连击、过渡），无需外部音效文件
- **CSS3**：响应式布局，支持桌面 / 平板 / 手机横竖屏

### 项目结构

```
unable-to-converge-game/
├── index.html              # 网页入口
├── style.css               # 全局样式（深色主题、响应式）
├── script.js               # 游戏主逻辑（~3300 行）
├── assets/
│   ├── backgrounds/        # AI 场景背景图（10 张）
│   ├── pixel-backgrounds/  # 像素风格背景（9 张）
│   └── audio/              # 背景音乐（day / night / ending）
├── tools/
│   └── build-exhibition-poster.py  # 展览海报生成脚本
└── output/
    ├── 无法收敛-项目汇报.pptx
    └── poster/             # 海报输出
```

### 设计特点

- **像素风格视觉**：所有 UI 元素采用像素化绘制（`image-rendering: pixelated`），角色头像由 Canvas 逐块绘制
- **拖拽交互系统**：物件可拖至目标区域，支持桌面鼠标拖拽和移动端触屏长按拖拽
- **连击奖励**：短时间内连续正确操作可触发连击（×3 / ×6 / ×10 / ×15），获得额外数值加成和视觉反馈
- **粒子特效**：关键操作时生成彩色粒子（成功=绿色，失败=红色，拖拽=金色尾迹）
- **过渡动画**：场景切换时带扫描线淡入淡出效果
- **决策日志**：完整记录每次操作的时间、数值变化和上下文
- **触摸涟漪**：移动端触屏操作有涟漪动画反馈
- **多场景背景**：9 张像素背景 + 1 张 AI 绘制结局背景，按场景自动切换
- **背景音乐**：白天/夜晚/结局三首 BGM，场景切换时自动过渡

## 快速开始

### 本地运行

1. 使用 VS Code 打开项目文件夹
2. 安装 Live Server 扩展
3. 右键 `index.html` → `Open with Live Server`
4. 或者直接双击 `index.html` 在浏览器中打开

### 部署到 GitHub Pages

1. 将项目文件上传到 GitHub 仓库根目录
2. 进入仓库 Settings → Pages
3. Source 选择 `Deploy from a branch`，Branch 选择 `main`，目录选择 `/root`
4. 保存后等待部署完成

### 生成展览海报

```bash
pip install Pillow
python tools/build-exhibition-poster.py
```

海报将输出到 `output/poster/` 目录（PNG + PDF + 预览图）。

## 创作理念

> 在可优化的系统之外，关系仍以不可解释的方式运行。

作品追问一个问题：当每一次"更详细地解释"都被对方听成新的证据、每一次"更委婉地表达"都被改写成新的亏欠时，**停止解释是否也可以成为边界的重构？**

白天的问题有步骤、有反馈、有完成——它们是可训练的、可收敛的。夜晚的误解却持续偏移，没有真实的标签，无法反向传播，无法校准。游戏不提供"正确的沟通方式"，只记录玩家在两种系统之间反复穿行的痕迹。

## 关于作者

- **艺术家**：陈维隆
- **在线游玩**：[sh1nyyythu.github.io/unable-to-converge-game](https://sh1nyyythu.github.io/unable-to-converge-game)
- **技术**：HTML / CSS / JavaScript / Canvas 2D / Python (Pillow)

---

*A playable study of expression, misreading and boundaries.*
